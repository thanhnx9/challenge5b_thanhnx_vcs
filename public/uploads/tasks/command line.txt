-T fields -e frame.number -e frame.protocols -e dns.qry.name -e http.host -Y 
tshark -r full.pcap -T fields -e dns.qry.name -e http.host > domain.txt
tshark -r full.pcap -T fields -e frame.time -e ip.src -e ip.dst -e dns.qry.name -e http.host > domain_2.txt
tshark -w

tshark -r tinba.pcap -T fields -e frame.time -e dns.time -e frame.time_delta -e ip.src -e ip.dst -e dns.qry.name -e http.host > domain1.xsl

tshark -r padcrypt.pcap -T fields -e _ws.col.AbsTime -e dns.time -e frame.time_delta -e ip.src -e ip.dst -e dns.qry.name -e http.host > domain.xsl

tshark -r tinba.pcap -T fields -Y "dns.flags.response == 0" -e frame.time -e dns.time -e frame.time_delta -e ip.src -e ip.dst -e dns.qry.name -e http.host > domain_queries.xsl   #export queries

tshark -r tinba.pcap -T fields -Y "dns.flags.response == 1 and dns.flags.rcode!=0" -e frame.time -e dns.time -e frame.time_delta -e ip.src -e ip.dst -e dns.qry.name -e http.host -e _ws.col.Info > domain_response.xsl #export response

pip install pcapy  --global-option="build_ext" --global-option="-Id:\Downloads\winpcap\WpdPack" --global-option="-Ld:\Downloads\winpcap\WpdPacklib\x64






<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/latest/css/bootstrap.min.css">



using  vintage-style-film-textures
rpm --import https://yum.mariadb.org/RPM-GPG-KEY-MariaDB
CÓ 2 loại mã nhận diện chính:
	+) Host-based signatures: là mã nhận diện được sử dụng để phát hiện mã độc trên máy tính
	+) Network  signatures: là các mã nhận diện được sử dụng để phát hiện mã độc khi giám sát hệ thống mạng

	
	
	
	+)Tiêu chí đánh giá<trong CSDL, FTP Server>
	+)Đánh giá theo các tiêu chí
	<có các bước kiểm định==> đánh giá theo từng bước>
	<có các yếu tố đánh giá==> đánh giá 1 hệ thống thực tế theo các yếu tố này>
	
	
	MK: 1234567890Thanh=> thanhnx.vnist@gmail.com